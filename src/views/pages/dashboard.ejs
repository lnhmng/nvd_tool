<%
    // Giả định dữ liệu được truyền vào từ Controller (WebController.getDashboard)
    // statusData, title, activeMenu, user đã được Controller cung cấp
%>

<% layout = 'layouts/main_layout' %>

<p>Test CSS path: /css/style.css</p>

<div class="dashboard-content">
    <div class="page-header-tabs">
        <span class="tab-item active">总览</span> <span class="tab-item">操作</span> </div>
    
    <div class="status-bar">
        <div class="status-card pending">
            <span class="icon">📜</span>
            <p style="color: red;">Danh sách chờ</p>
            <p class="count"><%= statusData.pendingCount %> Miếng</p>
        </div>
        <div class="status-card complete">
            <span class="icon">✅</span>
            <p>Danh sách đã hoàn thành</p>
            <p class="count"><%= statusData.completeCount %> Miếng</p>
        </div>
        <div class="status-card total">
            <span class="icon">🗓️</span>
            <p>Tổng danh sách đã hoàn thành</p>
            <p class="count"><%= statusData.totalComplete %> Miếng</p>
        </div>
        <div class="time-qr">
            <p>Thời gian</p>
            <p class="datetime"><%= statusData.currentDate %></p>
            <p class="datetime"><%= statusData.currentTime %></p>
                    </div>
    </div>

    <hr>

    <div class="process-map">
                <p class="map-title">HỆ THỐNG QUẢN LÝ THÔNG MINH</p>
    </div>
</div>